# Copy this file to .env and fill in your details before running docker compose up -d

# General
# Relative paths are resolved from the directory where docker-compose.yml lives.
DATA_ROOT=./data

# Optional profiles:
# - leave empty for local-only services
# - public-game enables Playit for game/public TCP or UDP exposure
# - public-http enables Traefik + Cloudflare Tunnel for domain/subdomain-based web apps
# Example: COMPOSE_PROFILES=public-game,public-http
COMPOSE_PROFILES=

# Cloudflare Tunnel token from Zero Trust dashboard.
# Optional: only needed if you enable the public-http profile and own a domain.
CLOUDFLARED_TOKEN=your_cloudflare_tunnel_token_here

# Single public host for path-based HTTP apps, for example:
# https://home.example.com/nextcloud
# https://home.example.com/openwebui
PUBLIC_APP_SCHEME=https
PUBLIC_APP_HOST=home.example.com
PUBLIC_BASE_URL=https://home.example.com

# Minecraft
MC_MEMORY=2G
MC_TYPE=VANILLA
MC_VERSION=LATEST

# Nextcloud
NEXTCLOUD_ADMIN_USER=admin
NEXTCLOUD_ADMIN_PASSWORD=change_me
# Space-separated list. Add your PUBLIC_APP_HOST if you enable public-http.
NEXTCLOUD_TRUSTED_DOMAINS=localhost 127.0.0.1 home.example.com
NEXTCLOUD_PUBLIC_PATH=/nextcloud
NEXTCLOUD_TRUSTED_PROXIES=127.0.0.1 172.16.0.0/12

# Open WebUI (set to a long random string, e.g. `openssl rand -hex 32`)
WEBUI_SECRET_KEY=replace_with_a_long_random_secret
OPENWEBUI_PUBLIC_PATH=/openwebui

# Playit Docker agent secret key.
# Choose the Docker option in Playit, then paste that key here.
# Optional: only needed if you enable the public-game profile.
PLAYIT_SECRET_KEY=your_playit_secret_key_here
